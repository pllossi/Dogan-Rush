<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Dogan_Rush.View.Rules"
             Title="Regole"
             BackgroundColor="#BEAA7C">

    <ScrollView>
        <VerticalStackLayout Padding="25" Spacing="25">

            <!-- Titolo Principale -->
            <Frame CornerRadius="12" BackgroundColor="#D9C9A5" Padding="15" HasShadow="True">
                <Label
                    Text="Regolamento"
                    FontSize="30"
                    HorizontalOptions="Center"
                    TextColor="#3B2F1D"
                    FontAttributes="Bold" />
            </Frame>

            <!-- Sezione 1 -->
            <Frame CornerRadius="12" BackgroundColor="#F5EEDC" Padding="20" HasShadow="True">
                <Label
                    Text="Nel gioco Dogan-Rush il tuo compito è controllare due documenti: una ID Card e una VISA Card. Entrambi devono contenere dati coerenti tra loro."
                    FontSize="18"
                    TextColor="#3B2F1D"
                    LineHeight="1.5" />
            </Frame>

            <!-- Sezione 2 -->
            <Frame CornerRadius="12" BackgroundColor="#F5EEDC" Padding="20" HasShadow="True">
                <Label
                    Text="Ogni documento ha una data di emissione e una di scadenza. La durata deve essere esattamente di 5 anni. Qualsiasi altra durata è considerata un errore."
                    FontSize="18"
                    TextColor="#3B2F1D"
                    LineHeight="1.5" />
            </Frame>

            <!-- Sezione 3 -->
            <Frame CornerRadius="12" BackgroundColor="#F5EEDC" Padding="20" HasShadow="True">
                <Label
                    Text="La data di nascita del viaggiatore deve essere coerente con la data attuale di gioco. Se è futura, il documento è non valido."
                    FontSize="18"
                    TextColor="#3B2F1D"
                    LineHeight="1.5" />
            </Frame>

            <!-- Sezione 4 -->
            <Frame CornerRadius="12" BackgroundColor="#F5EEDC" Padding="20" HasShadow="True">
                <Label
                    Text="Se tutti i dati sono corretti, premi il pulsante verde. Se c'è almeno un errore, premi il pulsante rosso. Hai tre vite: al terzo errore la partita finisce."
                    FontSize="18"
                    TextColor="#3B2F1D"
                    LineHeight="1.5" />
            </Frame>

            <!-- Chiusura -->
            <Label
                Text="Sarai all'altezza?"
                FontSize="22"
                FontAttributes="Italic"
                TextColor="#443920"
                HorizontalOptions="Center"
                Margin="0,10,0,30" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>